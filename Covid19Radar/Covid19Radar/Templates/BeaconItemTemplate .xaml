<?xml version="1.0" encoding="utf-8" ?>
<DataTemplate
    x:Class="Covid19Radar.Templates.BeaconItemTemplate"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <StackLayout Padding="15,0">
        <Label FontSize="Body" Text="{Binding BeaconUuid}">
            <Label.Margin>
                <Thickness
                    Bottom="{OnPlatform Android=0,
                                        iOS=5}"
                    Left="0"
                    Right="0"
                    Top="{OnPlatform Android=5,
                                     iOS=15}" />
            </Label.Margin>
        </Label>
        <Label FontSize="Body" Text="{Binding Major}">
            <Label.Margin>
                <Thickness
                    Bottom="{OnPlatform Android=0,
                                        iOS=5}"
                    Left="0"
                    Right="0"
                    Top="{OnPlatform Android=5,
                                     iOS=15}" />
            </Label.Margin>
        </Label>
        <Label FontSize="Body" Text="{Binding Minor}">
            <Label.Margin>
                <Thickness
                    Bottom="{OnPlatform Android=0,
                                        iOS=5}"
                    Left="0"
                    Right="0"
                    Top="{OnPlatform Android=5,
                                     iOS=15}" />
            </Label.Margin>
        </Label>
        <Label FontSize="Body" Text="{Binding Count}">
            <Label.Margin>
                <Thickness
                    Bottom="{OnPlatform Android=0,
                                        iOS=5}"
                    Left="0"
                    Right="0"
                    Top="{OnPlatform Android=5,
                                     iOS=15}" />
            </Label.Margin>
        </Label>

        <Label FontSize="Body" Text="{Binding Distance}">
            <Label.Margin>
                <Thickness
                    Bottom="{OnPlatform Android=0,
                                        iOS=5}"
                    Left="0"
                    Right="0"
                    Top="{OnPlatform Android=5,
                                     iOS=15}" />
            </Label.Margin>
        </Label>
        <Label FontSize="Body" Text="{Binding ElaspedTime}">
            <Label.Margin>
                <Thickness
                    Bottom="{OnPlatform Android=0,
                                        iOS=5}"
                    Left="0"
                    Right="0"
                    Top="{OnPlatform Android=5,
                                     iOS=15}" />
            </Label.Margin>
        </Label>
        <Label
            Margin="0,0,0,15"
            FontSize="Caption"
            Text="{Binding Role}"
            TextColor="{StaticResource Accent7Color}" />
        <BoxView Style="{StaticResource DefaultLineStyle}" />
    </StackLayout>
</DataTemplate>